export interface IUser {
  id: number;
  first_name: string;
  last_name: string;
  email: string;
  avatar: string;
}

export interface PeriodicElement {
  name: string;
  email: string;
  avatar: string;
}
